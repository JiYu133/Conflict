## 《Conflict》改枪系统设计总结

### 一、核心设计哲学

《Conflict》的改枪系统采用**物理驱动 + 配置驱动**的设计理念，区别于《逃离塔科夫》等传统游戏的数值驱动模式。

- **数值驱动（塔科夫）**：每个配件提供一组预设的固定数值加成（后坐力-1、人机+2）。这些数值由设计师手动配置，为了游戏平衡可以随意调整。
- **物理驱动（Conflict）**：每个配件的效果由底层物理公式实时计算得出。更换枪管不是“后坐力-1”，而是改变了枪管长度这个物理参数，该参数自动输入弹道和导气公式，实时计算出新的初速、导气延迟和射速。设计师只需填入真实物理数据，结果由公式自动推导。

### 二、模型拆分标准

所有枪械模型按以下三层逻辑拆分：

#### 2.1 基础平台（不可更换，所有配件的挂载基础）

即枪械的“身体”，合并所有不可更换的固定结构：

- 上/下机匣本体
- 弹匣井、扳机护圈
- 弹匣卡笋、空仓挂机释放钮、快慢机/保险拨片
- 护木固定环、枪背带环、抛壳窗防尘盖
- 枪管节套固定接口

导出为一个完整的 `Weapon_Receiver.glb` 文件。

#### 2.2 可更换外部配件

玩家在部署前可自由更换的部件，每个部件独立导出为 `.glb` 文件：

| 类别 | 说明 | 影响参数 |
|------|------|---------|
| **枪管总成** | 枪管+导气管+导气箍+准星+螺纹段 | `barrel_length`, `muzzle_velocity`, `weight` |
| **护木** | 上护木+下护木合并 | `weight`, `rail_slots` |
| **枪托** | 固定托/折叠托/伸缩托 | `weight`, `length_of_pull`, `foldable` |
| **握把** | 手枪式握把 | `weight`, `ergonomics` |
| **弹匣** | 30发标准/45发RPK/60发弹鼓 | `magazine_capacity`, `reload_time`, `weight` |
| **枪口装置** | 消焰器/制退器/消音器 | `recoil`, `muzzle_flash`, `sound_signature` |
| **机匣盖** | 带皮轨/加高皮轨/无导轨盖 | `sight_mount_type`, `weight` |
| **瞄具系统** | 机械照门/红点/全息/ACOG | `magnification`, `ads_speed` |
| **侧面导轨** | 皮卡汀尼侧轨附件 | 提供额外配件挂载点 |
| **前握把** | 垂直/斜角/手挡 | `recoil_recovery`, `weight` |
| **战术灯/激光** | 战术灯/激光指示器/组合装置 | `weight` |

#### 2.3 核心运动部件（不可更换，但需独立导出做动画）

这些零件玩家不会更换，但需要独立动画驱动：

| 部件 | 说明 |
|------|------|
| **枪机框组** | 枪机框+枪机头+活塞杆+导气活塞，自动循环动画 |
| **拉机柄** | 长条状金属杆+伸出的手柄，手动拉枪机动画 |
| **扳机** | 扣扳机时向后旋转，独立动画 |

#### 2.4 删除的零件

以下零件在游戏视角下不可见或太小，用法线贴图表现：

- 螺丝、销钉、铆钉、垫片
- 弹簧、击锤、击针、阻铁
- 膛线、弹匣内部托弹板和弹簧
- 机匣内部加强筋和卡扣

### 三、配件接口标准

每个可更换配件的原点必须设置在**连接面中心**。在 Godot 中，通过 `Marker3D` 定义挂载点位置。配件的物理参数通过 `WeaponConfig` 和 `AttachmentConfig` 资源文件配置，不依赖模型文件本身。

### 四、物理参数修改（无视觉变化，纯配置）

以下改装不涉及模型更换，仅通过修改配置参数实现：

| 改装项 | 影响参数 | 说明 |
|--------|---------|------|
| 更换复进簧 | `spring_strength` | 更强的弹簧加速枪机复位，提高射速但后座更猛 |
| 更换枪机质量 | `bolt_mass` | 更重的枪机降低射速但减少后座 |
| 更换缓冲器 | `recoil_recovery` | 更好的缓冲器加速后座恢复 |
| 调整导气孔 | `gas_delay` | 更大的导气孔加快枪机循环 |

### 五、弹药系统

- 支持六种弹种：普通弹、穿甲弹、曳光弹、亚音速弹、空尖弹、燃烧弹
- 每个弹匣独立计数，部署前可自行配置弹匣内的弹种序列
- 不同弹种影响初速、穿透力、伤害、弹道特性

### 六、设计优势

1. **物理驱动的故障系统**：枪械卡壳、哑弹等故障由物理公式推导，不是随机概率
2. **开放的配件生态**：新增配件只需一份配置文件，Mod 制作者无需改动代码
3. **跨武器物理复用**：物理公式通用，输入任何枪械参数都能正确模拟
4. **逻辑与视觉分离**：配件模型独立管理，代码和模型互不依赖

### 七、与塔科夫的核心区别

| 维度 | 塔科夫 | Conflict |
|------|--------|---------|
| 驱动方式 | 数值驱动 | 物理驱动 |
| 改装粒度 | 宏观部件级 | 宏观部件+微观物理级 |
| 配件效果 | 预设固定数值 | 物理公式实时计算 |
| 参数自定义 | 不支持 | 支持修改枪机质量、弹簧刚度等 |
| 生态开放性 | 封闭，等官方更新 | 开放，配置文件驱动 |
| 故障系统 | 无 | 有，物理推导 |

### 八、当前工作流程

1. 从工业 STEP 模型导入 Blender
2. 按上述标准拆分零件（基础平台/可更换配件/运动部件/删除）
3. 每个配件设置原点于连接面中心
4. 用 `精简` 修改器减面（保证关键轮廓不变形）
5. 导出为独立 `.glb` 文件
6. 在 Godot 中通过 `WeaponAttachmentManager` 装配